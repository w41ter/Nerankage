PROJECT(Nerangake)

CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

SET(CUSTOM_CXX_FLAGS "-std=c++17 -Werror")

SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${CUSTOM_CXX_FLAGS}")

INCLUDE_DIRECTORIES(./include)

ADD_SUBDIRECTORY(src)
